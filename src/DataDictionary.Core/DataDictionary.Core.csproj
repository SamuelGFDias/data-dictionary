<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Description>The diff and startup-synchronization engine for DataDictionary — compares the compiled enum manifest against the persisted dictionary and fails fast on destructive divergence. Zero ORM dependencies.</Description>
    <PackageTags>datadictionary;enum;synchronization</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\DataDictionary.Abstractions\DataDictionary.Abstractions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

</Project>
